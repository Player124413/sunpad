diff --git a/CMakeLists.txt b/CMakeLists.txt
index ecbfc47..317152b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -115,13 +115,22 @@ if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME)
     set(ENABLE_HWDB OFF CACHE BOOL "" FORCE)
     set(ENABLE_EVDEV OFF CACHE BOOL "" FORCE)
     set(ENABLE_WAYLAND ON CACHE BOOL "" FORCE)
-    set(ENABLE_EGL OFF CACHE BOOL "" FORCE)
+    if(NOT CMAKE_SYSTEM_NAME STREQUAL "Android")
+        # Android builds keep EGL enabled so the OpenGL ES backend stays
+        # buildable as a fallback renderer next to Vulkan.
+        set(ENABLE_EGL OFF CACHE BOOL "" FORCE)
+    endif()
     set(USE_SYSTEM_SDL3 OFF CACHE BOOL "" FORCE)
     # SDL bundles its own copy of the macOS hidapi backend
     if(APPLE)
         set(SDL_HIDAPI OFF CACHE BOOL "" FORCE)
     endif()
-    set(LINUX_LOCAL_DEV ON CACHE BOOL "" FORCE)
+    if(NOT CMAKE_SYSTEM_NAME STREQUAL "Android")
+        # Relocatable Sys next to the binary is a desktop layout. On Android
+        # it makes GetSysDirectory ignore File::SetSysDirectory and the game
+        # exits immediately (black screen, MainLoop exited).
+        set(LINUX_LOCAL_DEV ON CACHE BOOL "" FORCE)
+    endif()
     set(DISTRIBUTOR "ModernGekko" CACHE STRING "" FORCE)
     add_subdirectory(vendor/dolphin EXCLUDE_FROM_ALL)
     target_sources(core PRIVATE
@@ -247,7 +256,13 @@ if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME)
             vendor/dolphin/Source/Core/DolphinNoGUI/PlatformWin32.cpp)
         target_link_libraries(moderngekko PRIVATE dwmapi)
     endif()
-    if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+    if(CMAKE_SYSTEM_NAME STREQUAL "Android")
+        # SunPad's ANativeWindow platform for Android (Vulkan/OpenGL ES WSI)
+        target_sources(moderngekko PRIVATE
+            vendor/dolphin/Source/Core/DolphinNoGUI/PlatformAndroid.cpp)
+        target_compile_definitions(moderngekko PRIVATE MODERNGEKKO_HAVE_ANDROID=1)
+        target_link_libraries(moderngekko PRIVATE android log)
+    elseif(CMAKE_SYSTEM_NAME STREQUAL "iOS")
         # SunPad's CAMetalLayer platform for iOS (no macOS windowing APIs)
         target_sources(moderngekko PRIVATE
             vendor/dolphin/Source/Core/DolphinNoGUI/PlatformIOS.mm)
@@ -292,24 +307,29 @@ if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME)
         target_compile_definitions(moderngekko PRIVATE HAVE_X11=1)
     endif()
     if(TARGET PkgConfig::WAYLAND)
+        # All Wayland pieces are optional: a missing wayland-scanner or
+        # protocol XML must not fail the configure (SunPad's desktop build
+        # only generates the recompiled module and does not need Wayland).
         find_program(MODERNGEKKO_WAYLAND_SCANNER wayland-scanner
-            HINTS "${_moderngekko_sdk_usr}/bin" REQUIRED)
+            HINTS "${_moderngekko_sdk_usr}/bin")
         find_file(MODERNGEKKO_XDG_SHELL_XML xdg-shell.xml
             HINTS "${_moderngekko_sdk_usr}/share/wayland-protocols/stable/xdg-shell"
-                  "/usr/share/wayland-protocols/stable/xdg-shell"
-            REQUIRED)
+                  "/usr/share/wayland-protocols/stable/xdg-shell")
         find_file(MODERNGEKKO_XDG_DECORATION_XML xdg-decoration-unstable-v1.xml
             HINTS "${_moderngekko_sdk_usr}/share/wayland-protocols/unstable/xdg-decoration"
-                  "/usr/share/wayland-protocols/unstable/xdg-decoration"
-            REQUIRED)
+                  "/usr/share/wayland-protocols/unstable/xdg-decoration")
         find_file(MODERNGEKKO_IDLE_INHIBIT_XML idle-inhibit-unstable-v1.xml
             HINTS "${_moderngekko_sdk_usr}/share/wayland-protocols/unstable/idle-inhibit"
-                  "/usr/share/wayland-protocols/unstable/idle-inhibit"
-            REQUIRED)
+                  "/usr/share/wayland-protocols/unstable/idle-inhibit")
         find_library(MODERNGEKKO_WAYLAND_CURSOR_LIBRARY wayland-cursor
-            HINTS "${_moderngekko_sdk_lib}" REQUIRED)
+            HINTS "${_moderngekko_sdk_lib}")
         find_library(MODERNGEKKO_XKBCOMMON_LIBRARY xkbcommon
-            HINTS "${_moderngekko_sdk_lib}" REQUIRED)
+            HINTS "${_moderngekko_sdk_lib}")
+    endif()
+    if(TARGET PkgConfig::WAYLAND AND MODERNGEKKO_WAYLAND_SCANNER AND
+       MODERNGEKKO_XDG_SHELL_XML AND MODERNGEKKO_XDG_DECORATION_XML AND
+       MODERNGEKKO_IDLE_INHIBIT_XML AND MODERNGEKKO_WAYLAND_CURSOR_LIBRARY AND
+       MODERNGEKKO_XKBCOMMON_LIBRARY)
         set(_moderngekko_wayland_generated
             "${CMAKE_CURRENT_BINARY_DIR}/generated/wayland")
         set(_moderngekko_xdg_header
@@ -371,7 +391,9 @@ if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME)
             "${MODERNGEKKO_WAYLAND_CURSOR_LIBRARY}"
             "${MODERNGEKKO_XKBCOMMON_LIBRARY}")
         target_compile_definitions(moderngekko PRIVATE HAVE_WAYLAND=1)
-        target_compile_definitions(videovulkan PRIVATE HAVE_WAYLAND=1)
+        if(TARGET videovulkan)
+            target_compile_definitions(videovulkan PRIVATE HAVE_WAYLAND=1)
+        endif()
         if(MODERNGEKKO_FRONTEND_SDK)
             target_link_directories(moderngekko PRIVATE "${_moderngekko_sdk_lib}")
         endif()
@@ -403,7 +425,11 @@ target_link_libraries(moderngekko-module-info
     PRIVATE ModernGekko::Runtime
 )
 
-if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME)
+if(MODERNGEKKO_ENABLE_DOLPHIN_RUNTIME AND
+   NOT CMAKE_SYSTEM_NAME STREQUAL "Android")
+    # The SDL3-based desktop frontend (moderngekko-run/launcher/port) is not
+    # built on Android: the SunPad app hosts the runtime itself, and SDL3 is
+    # disabled there (Pipes-only input).
     if(TARGET SDL3-static)
         set(_moderngekko_sdl_target SDL3-static)
     elseif(TARGET SDL3::SDL3-shared)
diff --git a/src/runtime/dolphin_runtime.cpp b/src/runtime/dolphin_runtime.cpp
index 1ddc25c..1894bd7 100644
--- a/src/runtime/dolphin_runtime.cpp
+++ b/src/runtime/dolphin_runtime.cpp
@@ -34,6 +34,10 @@
 extern "C" void ModernGekkoSetIOSRenderSurface(void* surface);
 #endif
 
+#ifdef MODERNGEKKO_HAVE_ANDROID
+extern "C" void ModernGekkoSetAndroidRenderSurface(void* surface);
+#endif
+
 #include <algorithm>
 #include <array>
 #include <atomic>
@@ -100,9 +104,17 @@ std::vector<std::string> Host_GetPreferredLocales() { return {}; }
 void Host_PPCSymbolsChanged() {}
 void Host_PPCBreakpointsChanged() {}
 bool Host_UIBlocksControllerState() { return false; }
+#ifdef MODERNGEKKO_HAVE_ANDROID
+extern "C" void SunPadNativeLog(const char* msg);
+#endif
+
 void Host_Message(HostMessageID id) {
-  if (id == HostMessageID::WMUserStop && s_platform)
+  if (id == HostMessageID::WMUserStop && s_platform) {
+#ifdef MODERNGEKKO_HAVE_ANDROID
+    SunPadNativeLog("Host_Message: WMUserStop");
+#endif
     s_platform->Stop();
+  }
 }
 void Host_UpdateTitle(const std::string &) {
   if (!s_platform)
@@ -293,6 +305,10 @@ RuntimeCreateResult Runtime::Create(RuntimeConfig config) {
 
   if (impl->config.headless)
     impl->platform = Platform::CreateHeadlessPlatform();
+#ifdef MODERNGEKKO_HAVE_ANDROID
+  else
+    impl->platform = Platform::CreateAndroidPlatform();
+#endif
 #ifdef MODERNGEKKO_HAVE_IOS
   else
     impl->platform = Platform::CreateIOSPlatform();
@@ -320,6 +336,9 @@ RuntimeCreateResult Runtime::Create(RuntimeConfig config) {
                          "the requested Dolphin host platform is unavailable"}};
   }
 
+#ifdef MODERNGEKKO_HAVE_ANDROID
+  ModernGekkoSetAndroidRenderSurface(impl->config.render_surface);
+#endif
 #ifdef MODERNGEKKO_HAVE_IOS
   ModernGekkoSetIOSRenderSurface(impl->config.render_surface);
 #endif
@@ -341,29 +360,81 @@ RuntimeCreateResult Runtime::Create(RuntimeConfig config) {
   Config::SetBase(Config::GFX_SHADER_COMPILATION_MODE,
                   ShaderCompilationMode::AsynchronousUberShaders);
   Config::SetBase(Config::GFX_WAIT_FOR_SHADERS_BEFORE_STARTING, true);
-#ifdef MODERNGEKKO_HAVE_IOS
+#if defined(MODERNGEKKO_HAVE_IOS) || defined(MODERNGEKKO_HAVE_ANDROID)
   // Dolphin's ARM64 vertex loader generates executable host code. iOS forbids
-  // that JIT path, so use the portable software vertex loader.
+  // that JIT path and Android app code generation is equally restricted, so
+  // use the portable software vertex loader on both mobile platforms.
   Config::SetBase(Config::GFX_VERTEX_LOADER_TYPE, VertexLoaderType::Software);
   // Favor uninterrupted playback over the lower-latency desktop default.
   Config::SetBase(Config::MAIN_AUDIO_BUFFER_SIZE, 120);
   Config::SetBase(Config::MAIN_AUDIO_FILL_GAPS, true);
+#ifdef MODERNGEKKO_HAVE_ANDROID
+  Config::SetBase(Config::GFX_VERTEX_LOADER_TYPE, VertexLoaderType::Native);
+  // Adreno cannot link ubershaders. Dual-core (CPU vs GPU) is the
+  // official Dolphin Android default and is what makes the game playable.
+  Config::SetBase(Config::GFX_SHADER_COMPILATION_MODE,
+                  ShaderCompilationMode::Synchronous);
+  Config::SetBase(Config::GFX_WAIT_FOR_SHADERS_BEFORE_STARTING, false);
+  Config::SetBase(Config::GFX_SHADER_PRECOMPILER_THREADS, 1);
+  Config::SetBase(Config::GFX_SHADER_COMPILER_THREADS, 1);
+  Config::SetBase(Config::GFX_BACKEND_MULTITHREADING, false);
+  Config::SetBase(Config::GFX_PREFER_GLES, true);
+  Config::SetBase(Config::MAIN_CPU_THREAD, true);
+  Config::SetBase(Config::MAIN_SYNC_ON_SKIP_IDLE, false);
+  Config::SetBase(Config::MAIN_FAST_DISC_SPEED, true);
+  // GMS.ini: Sunshine needs CPU EFB reads and EFB copies in RAM.
+  Config::SetBase(Config::GFX_HACK_EFB_ACCESS_ENABLE, true);
+  Config::SetBase(Config::GFX_HACK_SKIP_EFB_COPY_TO_RAM, false);
+  Config::SetBase(Config::GFX_HACK_DEFER_EFB_COPIES, true);
+  Config::SetBase(Config::GFX_HACK_MISSING_COLOR_VALUE, 0u);
+  Config::SetBase(Config::GFX_PERF_QUERIES_ENABLE, true);
+  Config::SetBase(Config::GFX_ENHANCE_ARBITRARY_MIPMAP_DETECTION, true);
+  Config::SetBase(Config::GFX_ENABLE_GPU_TEXTURE_DECODING, false);
+  Config::SetBase(Config::GFX_HACK_COPY_EFB_SCALED, false);
+  Config::SetBase(Config::GFX_FAST_DEPTH_CALC, false);
+  Config::SetBase(Config::GFX_HACK_IMMEDIATE_XFB, true);
+  Config::SetBase(Config::GFX_HACK_SKIP_DUPLICATE_XFBS, true);
+  Config::SetBase(Config::GFX_VSYNC, false);
+  Config::SetBase(Config::GFX_MSAA, 1u);
+  Config::SetBase(Config::GFX_SSAA, false);
+  Config::SetBase(Config::GFX_ENABLE_PIXEL_LIGHTING, false);
+  Config::SetBase(Config::GFX_SHADER_CACHE, true);
+  Config::SetBase(Config::GFX_HACK_VI_SKIP, true);
+  Config::SetBase(Config::GFX_HACK_FAST_TEXTURE_SAMPLING, true);
+  Config::SetBase(Config::GFX_HACK_EFB_DEFER_INVALIDATION, true);
+  Config::SetBase(Config::GFX_HACK_EARLY_XFB_OUTPUT, true);
+  Config::SetBase(Config::GFX_ENHANCE_DISABLE_COPY_FILTER, true);
+  Config::SetBase(Config::GFX_ENHANCE_FORCE_TEXTURE_FILTERING,
+                  TextureFilteringMode::Default);
+  Config::SetBase(Config::GFX_ENHANCE_MAX_ANISOTROPY,
+                  AnisotropicFilteringMode::Force1x);
+  Config::SetBase(Config::MAIN_DSP_HLE, true);
+  Config::SetBase(Config::MAIN_SYNC_GPU, false);
+  Config::SetBase(Config::MAIN_RUSH_FRAME_PRESENTATION, true);
+  Config::SetBase(Config::MAIN_LOAD_GAME_INTO_MEMORY, false);
+  Config::SetBase(Config::MAIN_PRECISION_FRAME_TIMING, false);
+#endif
 #endif
   const std::vector<std::string> audio_backends =
       AudioCommon::GetSoundBackends();
   if (impl->config.headless) {
     impl->config.audio.backend = BACKEND_NULLSOUND;
   } else if (impl->config.audio.backend.empty() ||
-             !std::ranges::contains(audio_backends,
-                                    impl->config.audio.backend)) {
+             std::ranges::find(audio_backends,
+                               impl->config.audio.backend) ==
+                 audio_backends.end()) {
     constexpr std::array preferred_backends = {
 #ifdef MODERNGEKKO_HAVE_IOS
         BACKEND_COREAUDIO,
+#endif
+#ifdef MODERNGEKKO_HAVE_ANDROID
+        BACKEND_OPENSLES,
 #endif
         BACKEND_CUBEB, BACKEND_PULSEAUDIO, BACKEND_ALSA};
     const auto preferred =
         std::ranges::find_if(preferred_backends, [&](const char *backend) {
-          return std::ranges::contains(audio_backends, backend);
+          return std::ranges::find(audio_backends, backend) !=
+                 audio_backends.end();
         });
     impl->config.audio.backend =
         preferred != preferred_backends.end() ? *preferred : BACKEND_NULLSOUND;
