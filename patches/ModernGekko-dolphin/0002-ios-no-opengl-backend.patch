diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9f837eca31..355fe8f86b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -664,2 +664,3 @@ endif()
-if(NOT (WIN32 AND _M_ARM_64))
-  # OpenGL is available on all platforms except windows-arm64
+if(NOT (WIN32 AND _M_ARM_64) AND NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
+  # OpenGL is available on all platforms except windows-arm64 and iOS
+  # (Metal-only there; the AGL/EGL interfaces are not built).
@@ -718 +719 @@ endif()
-if(NOT ANDROID)
+if(NOT ANDROID AND NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
@@ -746 +747 @@ dolphin_find_optional_system_library(CURL Externals/curl)
-if(NOT ANDROID)
+if(NOT ANDROID AND NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
